name: project57
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flame: ^1.28.1
  uuid: ^4.5.1
  tuple: ^2.0.2
  vector_math: ^2.1.4
  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - "assets/images/textures/cobblestone.jpeg"
  shaders:
    - "assets/shaders/shadowandcandles.frag"

